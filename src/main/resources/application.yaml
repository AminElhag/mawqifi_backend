spring:
  datasource:
    url: "jdbc:mysql://localhost/mobile-app-ws?useSSL=false&allowMultiQueries=true&serverTimezone=UTC&allowPublicKeyRetrieval=True"
    ##jdbc:mysql://localhost/mobile-app-ws?useSSL=false&allowMultiQueries=true
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

otp:
  expiry:
    time: 10400000
